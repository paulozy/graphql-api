export class Warrior {
  id: string;
  name: string;
  weapon: string;

  constructor(id: string, name: string, weapon: string) {
    this.id = id;
    this.name = name;
    this.weapon = weapon;
  }
}
